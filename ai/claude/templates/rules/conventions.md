# Project Conventions

<!-- TODO: Add project-specific conventions Claude should always follow.
     Examples:
       - Use the repository pattern for all data access
       - All public APIs require request/response DTO classes
       - Error handling follows the Result<T, E> pattern
       - Generated code lives in internal/generated/ — never edit directly
-->
