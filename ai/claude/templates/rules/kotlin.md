---
paths:
  - "**/*.kt"
  - "**/*.kts"
---

# Kotlin (project-specific)

<!-- TODO: Add Kotlin/Spring conventions specific to this project.
     Examples:
       - Use Spring Data repositories — no raw JDBC in service layer
       - Database migrations managed by Flyway in src/main/resources/db/migration/
       - Use @Transactional at the service layer, not repository layer
       - All HTTP endpoints require corresponding integration tests
-->
