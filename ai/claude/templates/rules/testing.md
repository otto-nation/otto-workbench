---
paths:
  - "**/test*/**"
  - "**/*Test.*"
  - "**/*_test.*"
  - "**/*Spec.*"
  - "**/*spec.*"
  - "**/__tests__/**"
---

# Testing Conventions

<!-- TODO: Add project testing conventions.
     Examples:
       - Integration tests require Docker (TestContainers) — run Docker before testing
       - Use factory helpers in tests/helpers/ for test data setup
       - All API tests must cover both success and error cases
       - Prefer real dependencies over mocks in integration tests
-->
