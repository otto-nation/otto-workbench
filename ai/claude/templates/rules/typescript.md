---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

# TypeScript (project-specific)

<!-- TODO: Add TypeScript conventions specific to this project.
     Examples:
       - Use Zod for all runtime validation at API boundaries
       - State management via Zustand — no prop drilling beyond 2 levels
       - Components in src/components/ are presentational — no data fetching
       - Data fetching belongs in src/hooks/ using React Query
-->
