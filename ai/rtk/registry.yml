# RTK registry — token compression CLI installed via brew.
# Regenerate: generate-tool-context

meta:
  section: "AI Tooling"
  validation: none

tools:
  - name: rtk
    permission: true
    visibility: brief
    description: "CLI proxy that compresses command output to reduce LLM token consumption — auto-active via PreToolUse hook"
    docs: "https://www.rtk-ai.app/"
