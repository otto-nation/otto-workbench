label = AI tools
description = Install Claude Code, MCPs, agents, and coding guidelines
post_install = **AI tools** (if installed): run `task --global ai:setup` to configure your `AI_COMMAND` and tokens
