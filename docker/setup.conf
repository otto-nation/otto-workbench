label = Docker
description = Configure Docker runtime (macOS: Colima/OrbStack, Linux: native) and Testcontainers
post_install = **Docker** (if installed): start your runtime — `colima start` or OrbStack (macOS), or `sudo systemctl start docker` (Linux)
