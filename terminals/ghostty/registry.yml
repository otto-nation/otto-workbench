# Ghostty script registry — installed when the Ghostty sub-tool is set up.
# Regenerate: generate-tool-context

meta:
  section: "Ghostty Scripts"
  validation: bindir
  source: terminals/ghostty/bin

tools:
  - name: ghostty-terminfo-push
    permission: false
    visibility: hidden
    description: "Installs Ghostty's xterm-ghostty terminfo on a remote host — fixes 'Error opening terminal' over SSH"
